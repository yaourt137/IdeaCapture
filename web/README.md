# IdeaCapture Web 查看器

网页端查看器，用于查看 Supabase 中存储的想法。

## ✨ 特性

- ✅ **自动配置加载**: 从环境变量读取 Supabase 配置，无需手动输入
- ✅ **安全**: 使用 Publishable Key 而非 Service Role Key
- ✅ **实时搜索**: 支持按标题、内容、标签搜索
- ✅ **响应式设计**: 适配桌面和移动端
- ✅ **零配置**: 启动即用

## 🚀 快速开始

### 1. 配置环境变量

确保 `IdeaCapture/IdeaCapture/.env` 文件包含以下配置：

```bash
# Supabase 配置
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_PUBLISHABLE_KEY=sb_publishable_xxx...
```

### 2. 启动服务器

**方法一：使用启动脚本（推荐）**

```bash
cd IdeaCapture/web
./start.sh
```

**方法二：手动启动**

```bash
cd IdeaCapture/web

# 安装依赖（首次运行）
pip3 install fastapi uvicorn python-dotenv

# 启动服务器
python3 server.py
```

### 3. 访问网页

打开浏览器访问：
- **主页**: http://localhost:8080
- **API 文档**: http://localhost:8080/docs

## 📋 API 端点

### GET `/api/config`

获取 Supabase 配置信息

**响应示例**:
```json
{
  "supabase_url": "https://xxx.supabase.co",
  "supabase_key": "sb_publishable_xxx",
  "configured": true
}
```

### GET `/health`

健康检查端点

**响应示例**:
```json
{
  "status": "healthy",
  "service": "IdeaCapture Web Viewer"
}
```

## 🔧 工作原理

```
┌─────────────────┐
│   浏览器         │
└────────┬────────┘
         │ 1. 访问 /
         ▼
┌─────────────────┐
│  server.py      │  提供 index.html
│  (FastAPI)      │
└────────┬────────┘
         │ 2. 请求 /api/config
         ▼
┌─────────────────┐
│  读取 .env 文件  │  SUPABASE_URL
│                 │  SUPABASE_PUBLISHABLE_KEY
└────────┬────────┘
         │ 3. 返回配置
         ▼
┌─────────────────┐
│   浏览器         │  使用配置直接访问 Supabase
│                 │  GET /rest/v1/ideas
└─────────────────┘
```


## 📝 文件结构

```
web/
├── index.html      # 前端页面（已重构，无需输入配置）
├── server.py       # 后端服务（提供配置API）
├── start.sh        # 启动脚本
└── README.md       # 本文件
```

## 🛠️ 依赖项

- Python 3.7+
- FastAPI
- Uvicorn
- python-dotenv

安装命令：
```bash
pip3 install fastapi uvicorn python-dotenv
```

## 🔄 与其他组件的关系

```
iOS App (IdeaCapture)
    ↓ 保存想法
Supabase Database
    ↑ 读取想法
Web Viewer (本项目)
```
